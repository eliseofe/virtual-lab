{
  "schema": "vlab.terminal-report/2",
  "outcome": "Everything aligned to the new heterogeneity syntax",
  "owner_impact": "Metrics can now read True/False traits (e.g. count informed robots). The editor suggests group, rest_of_group, set_trait, equip, place and trait(...). The capability GPT and Claude see is named 'Traits by group'. Docs distinguish traits (fixed) from memory (changes). Your three Experiments now use True/False, Sergio's Experiment is migrated with the same teams, and every older stored version compiles again (old versions may no longer mean exactly what they did, as agreed). Test now: yes. Open Constant Bearing Flocking from the Showcase and run it; the setup message reads 'Groups: information: informed 80, uninformed 120'.",
  "next": {
    "kind": "epic_continues",
    "issue": 577,
    "text": "#577 release 5: statements (local names, if/elif/else) in the environment field function, the last part of the one-grammar work. It has not started yet."
  },
  "technical_evidence": [
    "Kernel: ControllerRuntime::scientific_private_state_is_bool; bool traits appear in the Metrics snapshot as booleans. New recorded reference 'traits-groups' (dimensions, nesting, True/False and numeric traits read by Controller and Metrics): informed count 6 and gain total 14 as designed; native and WebAssembly bit-identical; the other five references unchanged.",
    "Editor: Initialization and Controller autocomplete entries. Contract vlab.authoring/0.16, MCP 3.26.0, migration 20260926170000 (display name, contract versions).",
    "Equivalence harness: 28,989 cases, 0 differ. 717 tests pass; tsc, cargo tests and fmt clean."
  ]
}
