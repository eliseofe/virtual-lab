{
  "schema": "vlab.terminal-report/2",
  "outcome": "Shared capability candidates no longer disappear when a closure is resumed under normal privacy rules",
  "owner_impact": "A student or professor resuming a blocked Experiment now sees every capability candidate linked by evidence they are allowed to read, even when the underlying request belongs to another user and its private request row is correctly hidden. The fallback contains only sanitized shared candidate metadata and visible evidence; it does not reveal requester identity, private drafts, context, notes, or publication provenance. Candidate items remain unavailable to authoring. This repair makes no scientific judgment about Sergio's pending requests.",
  "next": {
    "kind": "scope_complete",
    "disposition": "closed",
    "lab_url": "https://eliseofe.github.io/virtual-lab/",
    "text": "The approved capability-request/MCP repair scope derived from Sergio's PR is complete and is being closed. No further Sergio-PR-derived technical repair is scheduled. Reviewing Sergio's scientific capability requests and resuming the paused aggregation RNG work are separate owner decisions; neither starts automatically."
  },
  "technical_evidence": [
    "Closure readback membership is now driven by caller-visible capability_request_evidence rather than by whichever private capability_requests rows survive RLS.",
    "Shared semantic candidates and contract deltas are reconstructed only from the same explicit sanitized field allowlists used by neutral workspace discovery.",
    "The reconstruction helper strips unexpected candidate fields defensively, so broadening a future database query cannot silently expose requester identity, private context, draft artifacts, publication identity, notes, or generalized_by through the fallback.",
    "Readable own/professor-visible request rows retain their normal full RLS-authorized details; only hidden requests use the sanitized fallback.",
    "Evidence with no readable request or candidate metadata remains represented by request ID plus visible evidence instead of disappearing.",
    "No RLS policy, request lifecycle, candidate availability, compiler acceptance, tool count, or authoring contract changes in this ticket."
  ],
  "close_issue": 464
}
