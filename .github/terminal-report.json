{
  "schema": "vlab.terminal-report/2",
  "outcome": "Capability request integrity repair prepared in source; Supabase rollout remains required",
  "owner_impact": "The candidate requires complete request coverage, rejects duplicate reuse entries before evidence can be lost, preserves matching revision snapshots, and retains shared candidate links during resume. These behaviors become available only after the SQL migration and experiment-mcp deployment; a Pages deployment alone does not activate them.",
  "next": {
    "kind": "scope_complete",
    "disposition": "closed",
    "lab_url": "https://eliseofe.github.io/virtual-lab/",
    "text": "The requested PR preparation scope is finished. Review and merge the candidate, then apply migration 20260921170000_capability_request_integrity.sql and deploy experiment-mcp 3.13.1 before verifying the request flow in the live Lab. Production request-flow verification is pending; the separate provenance PR #461 remains independent."
  },
  "technical_evidence": [
    "Three disposable PostgreSQL regression suites execute the real closure functions against a minimal relational fixture; they do not certify the complete Supabase schema or RLS policies.",
    "Readback unit tests cover private request rows omitted by RLS, shared semantic candidates and contract deltas, historical evidence, and links without readable metadata.",
    "Existing RLS policies remain in force; the resume fallback uses only readable candidate metadata and visible evidence.",
    "No Supabase production mutation, Edge Function deployment, or PR merge was performed while preparing this candidate.",
    "Historical lost evidence is not reconstructed, and requested scientific capabilities remain unavailable until separately implemented."
  ]
}
