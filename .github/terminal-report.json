{
  "schema": "vlab.terminal-report/2",
  "outcome": "Add the missing publication-provenance repair for MCP workspace discovery",
  "owner_impact": "The database migration restores the heterogeneous initialization capability bibliography from its original scientific request, allowing the existing MCP consistency guard to accept that capability. Applying the Supabase migration is required; a Pages deployment alone does not repair the live database.",
  "next": {
    "kind": "scope_complete",
    "disposition": "closed",
    "lab_url": "https://eliseofe.github.io/virtual-lab/",
    "text": "This bounded repair has no successor ticket. Apply the included Supabase migration through the trusted database deployment process, then verify authenticated read_workspace with include_workspace_index=true before reporting the production incident resolved."
  },
  "technical_evidence": [
    "Migration 20260921160000 restores only the missing initialization.per_agent_private_state_assignment publication link from the exact request recorded in issue #304.",
    "The migration preserves existing bibliography and rejects missing publication data or a source request bound to a different capability.",
    "A disposable PostgreSQL 15 database verified restoration, idempotency, absence of the dynamic capability, preservation of existing provenance, and rejection of missing/conflicting source data.",
    "All 495 browser/compiler tests passed locally, including a regression using the deployed capability bindings.",
    "No runtime semantics, Experiment artifacts, authentication, RLS policies, or MCP consistency rules are changed.",
    "Production Supabase migration application and authenticated workspace verification remain deployment requirements; they are not performed by Pages CI."
  ]
}
