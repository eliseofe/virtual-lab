name: CI and Pages deploy

on:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - '**/*.md'
      - 'docs/**'
      - '.github/workflows/**'
  pull_request:
    branches: [main]
    paths-ignore:
      - '*.md'
      - '**/*.md'
      - 'docs/**'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: virtual-lab-ci-pages-v3-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install frontend dependencies
        working-directory: web
        run: npm ci

      - name: Install Rust with WASM target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        uses: taiki-e/install-action@v2
        with:
          tool: wasm-pack

      - name: Test Rust workspace
        run: cargo test --workspace

      - name: Test browser/compiler contracts
        run: node --test web/tests/*.test.mjs

      - name: Typecheck frontend
        working-directory: web
        run: npm run typecheck

      - name: Build Rust kernel for browser
        working-directory: crates/kernel
        run: wasm-pack build --release --target web --out-dir ../../web/public/wasm --out-name vlab_kernel

      - name: Build static browser artifact
        working-directory: web
        run: npm run build

      - name: Verify browser artifact
        run: node web/scripts/verify-dist.mjs

      - name: Configure GitHub Pages
        if: github.event_name != 'pull_request'
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v4
        with:
          path: web/dist
          retention-days: 1

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  smoke:
    if: github.event_name != 'pull_request'
    needs: deploy
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout browser smoke scripts
        uses: actions/checkout@v6

      - name: Verify deployed browser reaches kernel ready with populated editors
        run: |
          URL="https://eliseofe.github.io/virtual-lab/"
          for attempt in 1 2 3 4 5 6; do
            if node web/scripts/browser-smoke.mjs "$URL"; then exit 0; fi
            echo "Browser smoke attempt $attempt failed; retrying after propagation delay."
            sleep 5
          done
          exit 1

      - name: Verify deployed React and Mantine boundary
        run: node web/scripts/frontend-foundation-smoke.mjs https://eliseofe.github.io/virtual-lab/

      - name: Verify deployed built-in metrics
        run: node web/scripts/builtin-metric-smoke.mjs https://eliseofe.github.io/virtual-lab/

      - name: Verify deployed live Results UI
        run: node web/scripts/results-ui-smoke.mjs https://eliseofe.github.io/virtual-lab/

      - name: Verify deployed local result persistence
        run: node web/scripts/result-persistence-smoke.mjs https://eliseofe.github.io/virtual-lab/

      - name: Verify deployed responsive hierarchy and focus behavior
        run: node web/scripts/responsive-smoke.mjs https://eliseofe.github.io/virtual-lab/

      - name: Verify deployed Showcase first-open UX
        run: node web/scripts/showcase-ux-smoke.mjs https://eliseofe.github.io/virtual-lab/
